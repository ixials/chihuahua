# Legacy RMS pipeline docs

Superseded by the PANNs-first plan in [`../bark_detection_plan.md`](../bark_detection_plan.md).

- `handoff.md` — session handoff from the RMS M0–M6 era
- `prompts/` — per-milestone executor prompts (m2–m7)

Legacy RMS stage outputs are written to `outputs/<stem>/legacy_rms/` (not the run root).
